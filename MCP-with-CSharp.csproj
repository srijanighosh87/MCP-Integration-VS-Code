<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net9.0</TargetFramework>
    <RootNamespace>MCP_with_CSharp</RootNamespace>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Hosting" Version="9.0.8" />
    <PackageReference Include="ModelContextProtocol" Version="0.3.0-preview.4" />
    <PackageReference Include="System.Text.Json" Version="9.0.8" />
  </ItemGroup>
  <PropertyGroup>
    <!-- The following properties are optional and can be used to enable container support for the SDK. -->
     <!-- <EnableSdkContainerSupport>true</EnableSdkContainerSupport>
     <ContainerRepository>jamesmontemagno/monkeymcp2</ContainerRepository>
     <ContainerFamily>alpine</ContainerFamily>
     <RuntimeIdentifier>linux-x64;linux-arm64</RuntimeIdentifier> -->
  </PropertyGroup>
</Project>
